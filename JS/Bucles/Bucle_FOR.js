
/* Visualizar los números pares entre 2 y 40 */

for(let numero=2; numero<=40 ; numero=numero+2 ) {
    console.log( numero );
  }


  for (let i = 0                 ; i < 10                 ; i++          ) {
    // | Declaramos una variable | Expresión condcicional | Incrementamos la variable |
        console.log(i);
    }
    //es un bucle infinito
